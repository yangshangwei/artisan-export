server:
     port: 8080

spring:
    datasource:
        name: test
        url: jdbc:postgresql://10.154.4.3:5432/userdb?useUnicode=true&characterEncoding=UTF-8&allowMultiQuer
        username: postgres
        password: cmss@123
        #  use druid datasource
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: org.postgresql.Driver


mybatis:
  mapper-locations: classpath:mapper/*.xml  
